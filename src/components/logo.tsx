"use client"
import Image from "next/image"

export default function Logo(): React.JSX.Element {
  return (
    <img
      src="/as-safar.png"
      style={{
        width: 100,
        height: "auto"
      }}
      alt=" "
    />
  )
}